// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['skartistic.com','dragonautomart.com','imgs.search.brave.com',] // Add the domain here
  },
}

module.exports = nextConfig;
